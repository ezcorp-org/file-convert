---
phase: 06-performance-bug-fixes
plan: 06
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05"]
files_modified:
  - apps/frontend/tests/e2e/error-handling/extension-spoofing.spec.ts
  - apps/frontend/tests/e2e/error-handling/file-validation-errors.spec.ts
  - apps/frontend/tests/e2e/error-handling/worker-crash-recovery.spec.ts
autonomous: true

must_haves:
  truths:
    - "All unskippable tests are enabled and passing"
    - "Full test suite runs with documented skip count"
    - "Bug fixes from earlier plans don't cause regressions"
  artifacts:
    - path: "apps/frontend/tests/e2e/error-handling/extension-spoofing.spec.ts"
      provides: "Updated spoofing tests after BUG-05 fix"
      contains: "test("
    - path: "apps/frontend/tests/e2e/error-handling/file-validation-errors.spec.ts"
      provides: "Updated validation tests"
      contains: "test("
  key_links:
    - from: "extension-spoofing.spec.ts"
      to: "file-validation.ts"
      via: "validateTextFormat"
      pattern: "validateTextFormat|magic.*byte"
---

<objective>
Enable skipped tests and verify full test suite stability

Purpose: BUG-07, BUG-08 from requirements - unskip tests that were blocked by bugs fixed in earlier plans, run full suite to ensure zero regressions.

Output: Updated test files with unskipped tests, documented skip count, clean test run.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-performance-bug-fixes/06-RESEARCH.md

# Skipped tests from audit (53 total)
# Priority tests to unskip after bug fixes:
# - extension-spoofing.spec.ts (3 skipped - BUG-05 fix enables these)
# - file-validation-errors.spec.ts (6 skipped)
# - worker-crash-recovery.spec.ts (2 skipped)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable extension spoofing tests after BUG-05 fix</name>
  <files>apps/frontend/tests/e2e/error-handling/extension-spoofing.spec.ts</files>
  <action>
Review and enable spoofing detection tests now that text format validation exists (BUG-05 fix from Plan 03):

1. Read the current skipped tests:
   - "detects JPEG file with PNG extension"
   - "detects PNG file with JPEG extension"
   - "detects WAV file with MP3 extension"

2. Evaluate each test:
   - If the test validates behavior that the BUG-05 fix enables, unskip it
   - If the test requires app-side integration (not just validation), keep skipped with updated comment

3. For tests that can be enabled:
   - Change `test.skip(...)` to `test(...)`
   - Update test logic if needed to use new validation

4. For tests that must stay skipped:
   - Update skip reason to be specific about what's missing
   - Add TODO comment with implementation path

5. Example update pattern:
```typescript
// Before (skipped pending validation implementation):
test.skip('detects JPEG file with PNG extension', async ({ page }) => {
  // ...
});

// After (enabled if app validates, or kept skipped with better reason):
test('detects JPEG file with PNG extension', async ({ page }) => {
  // Test that app rejects file with mismatched magic bytes
  // This requires FileUploader to call validateTextFormat()
  // ...
});

// OR if still blocked:
test.skip('detects JPEG file with PNG extension', async ({ page }) => {
  // TODO: Requires FileUploader.svelte to integrate magic byte validation
  // BUG-05 adds validation functions, but app must call them
});
```

6. Document which tests were enabled vs kept skipped
  </action>
  <verify>
Run: `cd apps/frontend && bunx playwright test tests/e2e/error-handling/extension-spoofing.spec.ts --project=chromium`
Note how many tests pass vs skip
  </verify>
  <done>Extension spoofing tests reviewed, enabled where possible, documented remaining blockers</done>
</task>

<task type="auto">
  <name>Task 2: Review and enable file validation error tests</name>
  <files>apps/frontend/tests/e2e/error-handling/file-validation-errors.spec.ts</files>
  <action>
Review file-validation-errors.spec.ts skipped tests (6 skipped):

1. Identify skipped tests:
   - "rejects PNG extension with JPEG magic bytes"
   - "rejects file with random bytes claiming to be JPEG"
   - "rejects file with random bytes claiming to be PNG"
   - "rejects GIF file exceeding 5MB limit"
   - "size limit error prevents file from being queued"
   - "rejects empty PNG file (0 bytes)"
   - "rejects empty JPEG file (0 bytes)"
   - "rejects empty JSON file (0 bytes)"

2. For each test, determine if it can be enabled:
   - Magic byte tests: Depends on app integration (likely still skipped)
   - Size limit tests: Depends on app enforcing limits (check if implemented)
   - Empty file tests: May be enableable if zero-byte check added

3. Enable tests that the bug fixes enable:
   - Change test.skip to test
   - Update assertions if behavior changed

4. Update skip reasons for remaining tests:
   - Be specific: "Blocked by: FileUploader doesn't call validateFile()"
   - Add implementation path

5. Run tests and verify no regressions from earlier passing tests
  </action>
  <verify>
Run: `cd apps/frontend && bunx playwright test tests/e2e/error-handling/file-validation-errors.spec.ts --project=chromium`
Note pass/skip count
  </verify>
  <done>File validation tests reviewed, enabled where possible, skip reasons updated</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and document results</name>
  <files>apps/frontend/tests/e2e/error-handling/worker-crash-recovery.spec.ts</files>
  <action>
Run full test suite and document final state:

1. First, review worker-crash-recovery.spec.ts skipped tests:
   - "shows retry indicator when worker retries" - likely still needs mocking
   - "detects pattern of repeated worker crashes" - needs session state mocking
   - Update skip reasons if unchanged

2. Run full E2E test suite:
```bash
cd apps/frontend && bunx playwright test --project=chromium
```

3. Document results:
   - Total tests
   - Passing tests
   - Skipped tests (with reasons)
   - Failed tests (investigate and fix or document)

4. For any new failures (regressions from bug fixes):
   - Investigate cause
   - Fix if simple
   - Document if complex

5. Create summary of test suite status:
   - How many skipped tests were enabled
   - Remaining skipped tests by category:
     - Missing app features (document conversion, FLAC UI, etc.)
     - Missing test infrastructure (mock workers, etc.)
     - External dependencies (OGG encoder, etc.)

6. Verify bug fixes didn't break existing functionality:
   - Memory leak fix (Plan 01)
   - Timeout fix (Plan 02)
   - Text validation (Plan 03)
  </action>
  <verify>
Run: `cd apps/frontend && bunx playwright test --project=chromium 2>&1 | tail -20`
Count passing, skipped, failed
  </verify>
  <done>Full test suite run documented, skip reasons updated, no regressions from bug fixes</done>
</task>

</tasks>

<verification>
1. Extension spoofing tests reviewed and updated
2. File validation tests reviewed and updated
3. Worker crash tests reviewed and updated
4. Full test suite runs with documented results
5. No regressions from Plans 01-05 bug fixes
6. Skip reasons are specific and actionable
</verification>

<success_criteria>
- BUG-07: Targeted tests added/enabled for fixed bugs
- BUG-08: Full test suite runs with documented status
- Skip count is minimized (only genuine blockers remain skipped)
- All passing tests from Phase 5 still pass
- Clear documentation of what's skipped and why
</success_criteria>

<output>
After completion, create `.planning/phases/06-performance-bug-fixes/06-06-SUMMARY.md`
</output>
