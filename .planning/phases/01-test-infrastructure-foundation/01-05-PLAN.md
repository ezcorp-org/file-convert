---
phase: 01-test-infrastructure-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/tests/convert-basic.spec.ts
  - apps/frontend/tests/convert-dropdown.spec.ts
  - apps/frontend/tests/convert-image.spec.ts
  - apps/frontend/tests/convert-manual.spec.ts
  - apps/frontend/tests/convert-page-working.spec.ts
  - apps/frontend/tests/convert-render.spec.ts
  - apps/frontend/tests/debug-conversion-issue.spec.ts
  - apps/frontend/tests/debug-file-upload.spec.ts
  - apps/frontend/tests/file-conversion-e2e-simple.spec.ts
  - apps/frontend/tests/file-conversion-e2e.spec.ts
  - apps/frontend/tests/file-conversion-working.spec.ts
  - apps/frontend/tests/format-detection.spec.ts
  - apps/frontend/tests/hamburger-simple.spec.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "13 debug/manual/duplicate test files are removed from the repository"
    - "CI workflow has been triggered and shows execution history"
    - "Test suite runs with fewer files (13 files removed)"
  artifacts:
    - path: "apps/frontend/tests/"
      provides: "Cleaned test directory with only valuable tests"
      expected_count: 14  # 24 root - 13 removed = 11 root + 3 e2e = 14 total
  key_links:
    - from: "git commit"
      to: ".github/workflows/e2e-tests.yml"
      via: "push event trigger"
      pattern: "on:.*push"
---

<objective>
Remove 13 debug/manual test files identified in TEST_AUDIT.md and trigger CI workflow to verify it runs

Purpose: Close Gap 1 (CI never executed) and prepare for migration by removing noise from test suite
Output: Clean test directory with 14 valuable files (11 root + 3 e2e), CI workflow execution history
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/frontend/tests/TEST_AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 13 debug/manual test files</name>
  <files>
    apps/frontend/tests/convert-basic.spec.ts
    apps/frontend/tests/convert-dropdown.spec.ts
    apps/frontend/tests/convert-image.spec.ts
    apps/frontend/tests/convert-manual.spec.ts
    apps/frontend/tests/convert-page-working.spec.ts
    apps/frontend/tests/convert-render.spec.ts
    apps/frontend/tests/debug-conversion-issue.spec.ts
    apps/frontend/tests/debug-file-upload.spec.ts
    apps/frontend/tests/file-conversion-e2e-simple.spec.ts
    apps/frontend/tests/file-conversion-e2e.spec.ts
    apps/frontend/tests/file-conversion-working.spec.ts
    apps/frontend/tests/format-detection.spec.ts
    apps/frontend/tests/hamburger-simple.spec.ts
  </files>
  <action>
Delete the 13 files marked REMOVE in TEST_AUDIT.md:

**Current baseline (verified):**
- 24 files in apps/frontend/tests/*.spec.ts
- 3 files in apps/frontend/tests/e2e/*.spec.ts
- Total: 27 test files

**Files to delete (13 total):**
1. convert-basic.spec.ts - Debug only, no assertions
2. convert-dropdown.spec.ts - Exploratory, page.evaluate anti-pattern
3. convert-image.spec.ts - page.evaluate anti-pattern for file uploads
4. convert-manual.spec.ts - Manual debugging, hardcoded URL
5. convert-page-working.spec.ts - Diagnostic test, not real test
6. convert-render.spec.ts - Debug with hardcoded URL
7. debug-conversion-issue.spec.ts - Debug test for non-existent page
8. debug-file-upload.spec.ts - Pure debug logging, no assertions
9. file-conversion-e2e-simple.spec.ts - Superseded by -fixed version
10. file-conversion-e2e.spec.ts - 512 lines of duplication, superseded
11. file-conversion-working.spec.ts - Superseded by -fixed version
12. format-detection.spec.ts - Debug test with page.evaluate
13. hamburger-simple.spec.ts - Duplicate of hamburger-fixed

Use `rm` to delete each file. Do NOT use git rm - just delete the files.

**Expected result after deletion:**
- 11 files in apps/frontend/tests/*.spec.ts (24 - 13 = 11)
- 3 files in apps/frontend/tests/e2e/*.spec.ts (unchanged)
- Total: 14 test files
  </action>
  <verify>
    ls apps/frontend/tests/*.spec.ts | wc -l  # Should return 11
    ls apps/frontend/tests/e2e/*.spec.ts | wc -l  # Should return 3
    # Total: 11 + 3 = 14 test files
  </verify>
  <done>
    - 13 files deleted
    - Test directory contains 11 spec files in root
    - e2e directory contains 3 spec files (unchanged)
    - Total: 14 test files (down from 27)
    - No debug/manual tests remain
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit cleanup and push to trigger CI</name>
  <files>.github/workflows/e2e-tests.yml</files>
  <action>
1. Stage all deleted files: `git add -u apps/frontend/tests/`
2. Commit with message explaining the cleanup
3. Push to main branch to trigger CI workflow

The push event will trigger .github/workflows/e2e-tests.yml which runs on push to main.

IMPORTANT: After pushing, wait 10-15 seconds then check GitHub Actions status using `gh run list`.
  </action>
  <verify>
    gh run list --limit 1  # Should show a new workflow run triggered by the push
    gh run view $(gh run list --limit 1 --json databaseId -q '.[0].databaseId') --log 2>/dev/null || echo "Run in progress"
  </verify>
  <done>
    - Commit created with test cleanup
    - Push triggered CI workflow
    - GitHub Actions shows workflow run (in progress or completed)
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify CI workflow completes successfully</name>
  <files>None (verification only)</files>
  <action>
Wait for CI workflow to complete and verify it passes:

1. Use `gh run watch` to monitor the workflow (or poll with `gh run view`)
2. If workflow fails, examine the logs to understand why
3. If failure is due to remaining test issues, document but don't block - Gap 2 (migration) will address

Expected CI behavior:
- Uses Bun (not npm)
- Caches Playwright browsers
- Runs with single worker
- Runs only on Chromium
- Uploads artifacts on failure

If the workflow takes more than 5 minutes, it's likely stuck - check the logs.
  </action>
  <verify>
    gh run list --limit 1 --json status,conclusion -q '.[0]'
    # Expected: {"status":"completed","conclusion":"success"} or at least status:completed
  </verify>
  <done>
    - CI workflow completed (success or failure with documented reason)
    - Gap 1 closed: CI has execution history proving it runs
    - If failed: Issues documented for Gap 2 migration to address
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. File count verification:
   - `ls apps/frontend/tests/*.spec.ts | wc -l` returns 11
   - `ls apps/frontend/tests/e2e/*.spec.ts | wc -l` returns 3
   - Total: 14 test files (down from 27)

2. CI verification:
   - `gh run list` shows at least one workflow run
   - Workflow status is "completed" (success or failure)

3. No regression:
   - Remaining tests can still be discovered by Playwright
   - `cd apps/frontend && npx playwright test --list` shows tests from remaining files
</verification>

<success_criteria>
- 13 debug/manual test files deleted
- Git commit pushed to main
- CI workflow triggered and completed (Gap 1 closed)
- 14 test files remain (11 root + 3 e2e) for migration in Plan 06
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure-foundation/01-05-SUMMARY.md`
</output>
