---
phase: 01-test-infrastructure-foundation
plan: 06
type: execute
wave: 2
depends_on: [01-05]
files_modified:
  - apps/frontend/tests/convert-page.spec.ts
  - apps/frontend/tests/file-conversion-e2e-fixed.spec.ts
  - apps/frontend/tests/file-convert.spec.ts
  - apps/frontend/tests/hamburger-fixed.spec.ts
  - apps/frontend/tests/convert-flow.spec.ts
  - apps/frontend/tests/convert-functionality.spec.ts
  - apps/frontend/tests/convert-functionality-fixed.spec.ts
  - apps/frontend/tests/TEST_PATTERNS.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "4 KEEP tests import { test, expect } from './fixtures'"
    - "convert-flow.spec.ts contains merged unique tests from convert-functionality files"
    - "convert-functionality.spec.ts and convert-functionality-fixed.spec.ts are deleted"
    - "TEST_PATTERNS.md documents fixture usage, anti-patterns, and migration examples"
  artifacts:
    - path: "apps/frontend/tests/*.spec.ts"
      provides: "Tests migrated to fixture system"
      pattern: "import.*from.*fixtures"
    - path: "apps/frontend/tests/TEST_PATTERNS.md"
      provides: "Documentation for INFRA-10 requirement"
      min_lines: 50
  key_links:
    - from: "apps/frontend/tests/*.spec.ts"
      to: "apps/frontend/tests/fixtures/index.ts"
      via: "import statement"
      pattern: "import \\{ test, expect \\} from '\\.\\./fixtures'"
---

<objective>
Migrate 4 KEEP tests to fixtures, merge convert-functionality tests into convert-flow, and document test patterns

Purpose: Close Gap 2 (partial) - migrate highest-quality tests first and document patterns for INFRA-10
Output: 4 KEEP tests using fixtures, merged convert-flow, TEST_PATTERNS.md documentation
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@apps/frontend/tests/TEST_AUDIT.md
@apps/frontend/tests/fixtures/index.ts
@apps/frontend/tests/fixtures/file-helpers.ts
@apps/frontend/tests/fixtures/download-helpers.ts
@apps/frontend/tests/fixtures/worker-lifecycle.ts
@apps/frontend/tests/fixtures/timeout-config.ts
@.planning/phases/01-test-infrastructure-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 4 KEEP tests to fixtures</name>
  <files>
    apps/frontend/tests/convert-page.spec.ts
    apps/frontend/tests/file-conversion-e2e-fixed.spec.ts
    apps/frontend/tests/file-convert.spec.ts
    apps/frontend/tests/hamburger-fixed.spec.ts
  </files>
  <action>
Migrate the 4 KEEP tests to use fixtures. These are already high-quality, so changes are minimal:

**For each file:**
1. Change import from `import { test, expect } from '@playwright/test'` to `import { test, expect } from './fixtures'`
2. Remove any waitForTimeout calls - replace with web-first assertions
3. Use fixture helpers where applicable (fileHelper for uploads, downloadHelper for downloads)

**Specific guidance:**

**convert-page.spec.ts** (clean page load tests)
- Just change import, minimal other changes needed
- No file operations, so fileHelper/downloadHelper not needed

**file-conversion-e2e-fixed.spec.ts** (best practice reference)
- Change import
- File upload already uses proper buffers - could use fileHelper.uploadFile() but existing pattern is fine
- Download handling could use downloadHelper.waitForDownload() for consistency
- Remove any waitForTimeout calls (check lines ~46)

**file-convert.spec.ts** (comprehensive UI/UX)
- Change import
- Replace waitForTimeout with `await expect(element).toBeVisible()` or similar
- No conversion operations, so worker fixtures not needed

**hamburger-fixed.spec.ts** (mobile nav)
- Change import
- Already well-written, minimal changes needed

After each file, run: `npx playwright test <filename> --reporter=list` to verify it still works.
  </action>
  <verify>
    cd apps/frontend && npx playwright test convert-page.spec.ts file-conversion-e2e-fixed.spec.ts file-convert.spec.ts hamburger-fixed.spec.ts --reporter=list
  </verify>
  <done>
    - 4 KEEP files import from ./fixtures
    - Zero waitForTimeout in these files
    - All 4 tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Merge convert-functionality tests into convert-flow</name>
  <files>
    apps/frontend/tests/convert-flow.spec.ts
    apps/frontend/tests/convert-functionality.spec.ts
    apps/frontend/tests/convert-functionality-fixed.spec.ts
  </files>
  <action>
Merge the 3 overlapping convert-functionality files into convert-flow.spec.ts.

**Step 1: Identify unique tests in convert-functionality files**

Read all 3 files and create a test name inventory:

convert-flow.spec.ts tests (these are the baseline):
- List all test('...') names

convert-functionality.spec.ts tests:
- List all test('...') names
- Mark which ones are NOT in convert-flow (these are unique, should be merged)

convert-functionality-fixed.spec.ts tests:
- List all test('...') names
- Mark which ones are NOT in convert-flow (these are unique, should be merged)

**Step 2: Merge unique tests**

For each unique test identified in Step 1:
1. Copy the test block to convert-flow.spec.ts
2. Update imports to use `import { test, expect } from './fixtures'`
3. Remove any waitForTimeout calls in the merged test

**Uniqueness criteria:**
- Test is unique if its name does NOT appear in convert-flow.spec.ts
- If test names are similar but different (e.g., "upload file" vs "upload file correctly"), compare assertions - if they test the same behavior, keep only one
- When in doubt, keep the more comprehensive version (more assertions = more valuable)

**Step 3: Update convert-flow.spec.ts**

1. Change import to `import { test, expect } from './fixtures'`
2. Remove ALL waitForTimeout calls - replace with web-first assertions
3. Anti-patterns to fix (from audit - lines 55, 92, 143):
   - Replace `page.waitForTimeout(500)` with `await expect(locator).toBeVisible()` or `await locator.waitFor()`

**Step 4: Delete duplicate files**

```bash
rm apps/frontend/tests/convert-functionality.spec.ts
rm apps/frontend/tests/convert-functionality-fixed.spec.ts
```

**DO NOT use waitForTimeout anywhere - this is the key anti-pattern we're fixing.**
  </action>
  <verify>
    cd apps/frontend && npx playwright test convert-flow.spec.ts --reporter=list
    # Verify convert-functionality files are deleted
    ls apps/frontend/tests/convert-functionality*.spec.ts 2>/dev/null && echo "ERROR: files still exist" || echo "OK: files deleted"
  </verify>
  <done>
    - convert-flow.spec.ts migrated to fixtures with zero waitForTimeout
    - Unique tests from convert-functionality files merged into convert-flow
    - convert-functionality.spec.ts deleted
    - convert-functionality-fixed.spec.ts deleted
    - Combined test passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Create TEST_PATTERNS.md documentation (INFRA-10)</name>
  <files>
    apps/frontend/tests/TEST_PATTERNS.md
  </files>
  <action>
Create documentation for test infrastructure patterns to satisfy INFRA-10 requirement.

Create `apps/frontend/tests/TEST_PATTERNS.md` with the following sections:

**1. Fixture Usage**
- How to import: `import { test, expect } from './fixtures'`
- Available fixtures: fileHelper, downloadHelper, workerLifecycle
- When to use each fixture

**2. Dynamic Timeout Configuration (INFRA-10)**
- Explain calculateTimeout() from timeout-config.ts
- Formula: base + (fileSizeMB * 2s * complexity)
- Examples: small file (30s), large image (45s), complex PDF (90s)
- How to use with testInfo.setTimeout()

**3. Anti-Patterns to Avoid**
- waitForTimeout - why it's bad, what to use instead
- page.evaluate for file uploads - use setInputFiles with buffers
- Manual visibility checks - use web-first assertions
- Hardcoded URLs - use relative paths

**4. Migration Examples**
Show before/after for common patterns:
- waitForTimeout -> web-first assertion
- page.evaluate file upload -> setInputFiles
- Manual isVisible() -> expect().toBeVisible()

**5. Worker Lifecycle**
- When to use workerLifecycle.waitForWorkerReady()
- Cleanup patterns (automatic in fixture teardown)

Document should be ~75-100 lines, practical and concise.
  </action>
  <verify>
    cat apps/frontend/tests/TEST_PATTERNS.md | wc -l  # Should be 50+ lines
    grep -q "calculateTimeout" apps/frontend/tests/TEST_PATTERNS.md && echo "Has timeout docs" || echo "Missing timeout docs"
    grep -q "waitForTimeout" apps/frontend/tests/TEST_PATTERNS.md && echo "Has anti-pattern docs" || echo "Missing anti-pattern docs"
  </verify>
  <done>
    - TEST_PATTERNS.md created with 50+ lines
    - Documents fixture usage patterns
    - Documents dynamic timeout configuration (INFRA-10)
    - Documents anti-patterns to avoid
    - Includes migration examples
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. KEEP tests use fixtures:
   ```bash
   grep -l "from './fixtures'" apps/frontend/tests/convert-page.spec.ts apps/frontend/tests/file-conversion-e2e-fixed.spec.ts apps/frontend/tests/file-convert.spec.ts apps/frontend/tests/hamburger-fixed.spec.ts | wc -l
   # Should return 4
   ```

2. convert-functionality files deleted:
   ```bash
   ls apps/frontend/tests/convert-functionality*.spec.ts 2>/dev/null
   # Should return nothing
   ```

3. TEST_PATTERNS.md exists and has content:
   ```bash
   test -f apps/frontend/tests/TEST_PATTERNS.md && echo "Exists" || echo "Missing"
   ```

4. Tests pass:
   ```bash
   cd apps/frontend && npx playwright test convert-page.spec.ts file-conversion-e2e-fixed.spec.ts file-convert.spec.ts hamburger-fixed.spec.ts convert-flow.spec.ts --reporter=list
   ```
</verification>

<success_criteria>
- 4 KEEP tests import from ./fixtures
- convert-flow.spec.ts contains merged unique tests, imports from ./fixtures
- convert-functionality.spec.ts and convert-functionality-fixed.spec.ts deleted
- TEST_PATTERNS.md documents fixture usage, dynamic timeouts (INFRA-10), and anti-patterns
- All migrated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure-foundation/01-06-SUMMARY.md`
</output>
