---
phase: 04-comprehensive-format-coverage
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/static/workers/audio-worker.js
  - apps/frontend/static/lib/lamejs.min.js
  - apps/frontend/static/lib/libflac.min.js
  - apps/frontend/tests/e2e/conversion/audio-conversions.spec.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Audio encoding tests run without CDN loading errors"
    - "MP3 conversion produces valid MP3 file with correct magic bytes"
    - "FLAC conversion produces valid FLAC file with correct magic bytes"
    - "Lossless round-trip test (WAV->FLAC->WAV) validates data preservation"
  artifacts:
    - path: "apps/frontend/static/lib/lamejs.min.js"
      provides: "Bundled lamejs encoder library"
      min_lines: 100
    - path: "apps/frontend/static/lib/libflac.min.js"
      provides: "Bundled libflac.js encoder library"
      min_lines: 100
    - path: "apps/frontend/static/workers/audio-worker.js"
      provides: "Audio worker with local library loading"
      contains: "/lib/lamejs.min.js"
  key_links:
    - from: "apps/frontend/static/workers/audio-worker.js"
      to: "/lib/lamejs.min.js"
      via: "importScripts or fetch from same origin"
      pattern: "lamejs\\.min\\.js"
    - from: "apps/frontend/tests/e2e/conversion/audio-conversions.spec.ts"
      to: "audio-worker.js"
      via: "active tests (not skipped)"
      pattern: "test\\("
---

<objective>
Bundle audio encoder libraries (lamejs, libflac.js) as static files to eliminate CDN loading failures in Playwright test environment.

Purpose: Resolve the primary verification gap blocking 3/9 success criteria (audio conversions, lossless validation, metadata preservation).

Output: Working MP3 and FLAC encoding in both production and test environments, with 14 audio tests unskipped and passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gap source
@.planning/phases/04-comprehensive-format-coverage/04-VERIFICATION.md

# Current implementation (CDN-based)
@apps/frontend/static/workers/audio-worker.js

# Test file to unskip
@apps/frontend/tests/e2e/conversion/audio-conversions.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle encoder libraries as static files</name>
  <files>
    apps/frontend/static/lib/lamejs.min.js
    apps/frontend/static/lib/libflac.min.js
  </files>
  <action>
Download and bundle the encoder libraries:

1. Create `apps/frontend/static/lib/` directory if it doesn't exist

2. Download lamejs from unpkg.com:
   - URL: https://unpkg.com/lamejs@1.2.1/lame.all.js
   - Save as: `apps/frontend/static/lib/lamejs.min.js`
   - Verify file contains `lamejs` global assignment

3. Download libflac.js from jsdelivr:
   - URL: https://cdn.jsdelivr.net/npm/libflacjs@5.4.0/dist/libflac.min.js
   - Save as: `apps/frontend/static/lib/libflac.min.js`
   - Verify file contains `Flac` global assignment

Both files are UMD/global scripts that assign to self.lamejs and self.Flac respectively.
This bundles ~374KB total but eliminates all CDN reliability issues.
  </action>
  <verify>
    - File exists: `ls -la apps/frontend/static/lib/lamejs.min.js`
    - File exists: `ls -la apps/frontend/static/lib/libflac.min.js`
    - lamejs file size > 150KB (expected ~174KB)
    - libflac file size > 150KB (expected ~200KB)
  </verify>
  <done>Both encoder libraries bundled as static files in apps/frontend/static/lib/</done>
</task>

<task type="auto">
  <name>Task 2: Update audio-worker.js to load from local files</name>
  <files>apps/frontend/static/workers/audio-worker.js</files>
  <action>
Modify `loadLameEncoder()` and `loadFLACEncoder()` methods to load from local static files:

1. In `loadLameEncoder()`:
   - Change CDN URL from `https://unpkg.com/lamejs@1.2.1/lame.all.js` to `/lib/lamejs.min.js`
   - Keep fetch + eval pattern (same-origin files work in Web Workers)
   - Update error messages to reflect local loading

2. In `loadFLACEncoder()`:
   - Change CDN URL from `https://cdn.jsdelivr.net/npm/libflacjs@5.4.0/dist/libflac.min.js` to `/lib/libflac.min.js`
   - Keep fetch + eval pattern
   - Update error messages to reflect local loading

3. Add fallback comment noting that local files eliminate CDN issues:
   ```javascript
   // Libraries bundled locally to avoid CDN fetch issues in test environments
   // lamejs: /lib/lamejs.min.js (~174KB)
   // libflac: /lib/libflac.min.js (~200KB)
   ```

Do NOT change any other logic - only the URLs in fetch() calls.
  </action>
  <verify>
    - `grep -n "lib/lamejs" apps/frontend/static/workers/audio-worker.js` shows local path
    - `grep -n "lib/libflac" apps/frontend/static/workers/audio-worker.js` shows local path
    - No CDN URLs remain: `grep -c "unpkg.com\|jsdelivr.net" apps/frontend/static/workers/audio-worker.js` returns 0
  </verify>
  <done>audio-worker.js loads encoder libraries from local /lib/ paths instead of CDN</done>
</task>

<task type="auto">
  <name>Task 3: Unskip and run audio conversion tests</name>
  <files>apps/frontend/tests/e2e/conversion/audio-conversions.spec.ts</files>
  <action>
Update the audio conversion tests to remove .skip and run against local encoder libraries:

1. Remove test.skip from the following tests (change `test.skip(` to `test(`):
   - "converts WAV to MP3" (line ~60)
   - "converts WAV to FLAC" (line ~144)
   - "WAV to FLAC to WAV is truly lossless" (line ~215)
   - "MP3 conversion maintains reasonable quality" (line ~266)

2. Update the file header comment to reflect resolved status:
   ```typescript
   /**
    * Audio conversion tests
    *
    * STATUS: MP3 and FLAC encoding working via bundled libraries
    * - lamejs: /lib/lamejs.min.js for MP3 encoding
    * - libflac.js: /lib/libflac.min.js for FLAC encoding
    *
    * OGG Vorbis and Opus remain blocked - no browser-compatible encoders available
    */
   ```

3. Keep these tests skipped (documented blockers - no encoder available):
   - "converts WAV to OGG"
   - "converts WAV to Opus"
   - All "Other Sources" tests (FLAC/MP3/OGG source - AudioFactory only creates WAV)

4. For the lossless round-trip test, implement the full test logic:
   - Upload WAV, convert to FLAC, download
   - Upload FLAC result, convert back to WAV, download
   - Compare sample count and duration between original and final
   - Assert duration difference < 0.01s
  </action>
  <verify>
    Run audio tests:
    ```
    cd apps/frontend && bunx playwright test audio-conversions.spec.ts --reporter=list
    ```
    Expected: At least 4 tests run (not skipped), MP3 and FLAC tests pass
  </verify>
  <done>Audio conversion tests unskipped and passing for MP3/FLAC encoding</done>
</task>

</tasks>

<verification>
1. Start dev server: `cd apps/frontend && bun run dev`
2. Run audio conversion tests:
   ```
   cd apps/frontend && bunx playwright test audio-conversions.spec.ts --reporter=list
   ```
3. Verify at least 4 tests pass (WAV->MP3, WAV->FLAC, lossless round-trip, MP3 quality)
4. Verify no CDN-related errors in test output
5. Verify MP3 output has correct magic bytes: 0xFF 0xFB or ID3 header
6. Verify FLAC output has correct magic bytes: 0x66 0x4C 0x61 0x43 (fLaC)
</verification>

<success_criteria>
1. lamejs.min.js and libflac.min.js bundled in static/lib/
2. audio-worker.js loads from local paths (no CDN URLs)
3. At least 4 audio tests unskipped and passing
4. WAV->MP3 conversion produces valid MP3 file
5. WAV->FLAC conversion produces valid FLAC file
6. Lossless round-trip (WAV->FLAC->WAV) preserves audio data
</success_criteria>

<output>
After completion, create `.planning/phases/04-comprehensive-format-coverage/04-13-SUMMARY.md`
</output>
